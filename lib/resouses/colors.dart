import 'package:flutter/material.dart';

const gradient = LinearGradient(
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    colors: [
      Color.fromRGBO(128, 119, 228, 1.0),
      Color.fromRGBO(241, 180, 136, 1.0),
    ]);

const white = Color.fromRGBO(246, 246, 246, 1.0);

const purple = Color.fromRGBO(140, 132, 226, 1.0);

const black = Color.fromRGBO(58, 58, 85, 1.0);

const orange = Color.fromRGBO(241, 180, 136, 1.0);

const orangeLight = Color.fromRGBO(241, 180, 136, 0.9);

const blue = Color.fromRGBO(103, 139, 210, 0.7);

const green = Color.fromRGBO(113, 165, 159, 0.7);

const shadow = Color.fromRGBO(0, 0, 0, 0.11);

const shadowForDrawer = Color.fromRGBO(0, 0, 0, 0.25);

const shadowCircular = Color.fromRGBO(128, 119, 231, 0.2);

const shadowSplashScreen = Color.fromRGBO(0, 0, 0, 0.08);

const blackBottomBar = Color.fromRGBO(58, 58, 85, 0.8);

const whiteBottomBar = Color.fromRGBO(0, 0, 0, 0.15);

const lightBlack = Color.fromRGBO(58, 58, 85, 0.5);

const greenCircular = Color.fromRGBO(113, 165, 159, 1);

const blueCircular = Color.fromRGBO(94, 119, 206, 1);

const purpleCircularShadow = Color.fromRGBO(255, 218, 121, 0.2);

const greenCircularShadow = Color.fromRGBO(129, 167, 111, 0.2);

const blueCircularShadow = Color.fromRGBO(94, 119, 206, 0.2);

const pictureShadow = Color.fromRGBO(0, 0, 0, 0.17);

const red = Color.fromRGBO(226, 119, 119, 1);

const blackCursor = Color.fromRGBO(64, 64, 64, 1);

const premiumShadow = Color.fromRGBO(0, 0, 0, 0.1);

const editcolor = Color.fromRGBO(0, 0, 0, 0.5);
